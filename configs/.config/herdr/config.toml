# herdr config — tmux 設定 (home-manager/programs/tmux/) の主要要素を移植したもの。
# 上書きした項目のみ列挙し、それ以外は herdr のデフォルト
# (https://herdr.dev/docs/configuration/) をそのまま使う。

onboarding = false

[terminal]
# 現状の login shell と揃える。shell_mode はデフォルトの "auto" のまま。
default_shell = "fish"

[session]
# tmux-continuum の `set -g @continuum-restore 'on'` に相当。
# detach / 再ログイン後もエージェントを状態ごと復元する。
resume_agents_on_restore = true

[keys]
# tmux の `prefix = "C-j"` を継承。
prefix = "ctrl+j"

# tmux `bind | split-window -h` — 縦分割 (画面を左右に割る) を `|` に。
# herdr の key parser には `bar` alias が無く、`|` は Shift+`\` として渡す必要が
# ある (src/config/keybinds.rs の parse_key_combo 参照)。配列でデフォルトの
# `prefix+v` も残す。
split_vertical = ["prefix+shift+backslash", "prefix+v"]

# tmux `bind -r C-h select-window -t :-` / `C-l` -> 次の window。
# herdr のタブ = tmux の window。
previous_tab = "prefix+ctrl+h"
next_tab = "prefix+ctrl+l"

# tmux `bind s` (tcmux switch-session) に相当する workspace 選択。
# 純正の choose-tree ではなく fuzzy picker が開く。
workspace_picker = "prefix+s"

# tmux `bind C new-session` — workspace = herdr の session 概念。
new_workspace = "prefix+shift+c"

# tmux `bind r source-file ~/.config/tmux/tmux.conf` の herdr 版。
[[keys.command]]
key = "prefix+r"
type = "shell"
command = "herdr server reload-config"
description = "reload herdr config"

[ui]
# tmux-agent-sidebar の情報密度を herdr で再現するため、
# ペイン境界に検出したエージェント名を出す。
show_agent_labels_on_pane_borders = true

# gap を切って compact divider にする。デフォルトの `true` はペイン間に空セルの
# gap + 各ペインの薄い枠、という構成でボーダー位置が視覚的にぼやけるため、
# 単一の divider だけにする。
pane_gaps = false

agent_panel_sort = "spaces"
[ui.toast]
# エージェントが blocked / done になった時などに herdr 上で通知する。
# terminal / system 通知は好みで切り替える。
delivery = "herdr"
